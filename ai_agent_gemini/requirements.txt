# ─────────────────────────────────────────────────────────
# 차트 왜곡 탐지 시스템 — 의존 패키지 (Gemini 3.5 Flash 버전)
# Python 3.11+
# pip install -r requirements.txt
# 실행 전: export GOOGLE_API_KEY="AIzaSy..."
#
# 모델 선택 (선택사항):
#   export CHART_MODEL=gemini-3.5-flash        # 기본값
#   export CHART_MODEL=gemini-2.5-pro          # 최고 정확도
#
# 추론 수준 (선택사항, Gemini 3.5 전용):
#   export GEMINI_THINKING_LEVEL=medium        # minimal|low|medium|high
# ─────────────────────────────────────────────────────────

# ── LangChain / LangGraph (멀티에이전트 그래프) ───────────
langchain>=1.2.17
langchain-core>=1.3.2
langchain-google-genai>=2.5.0   # Gemini 3.5 Flash 지원 (>=2.5.0 권장)
langchain-chroma>=1.1.0
langgraph>=1.1.10

# ── Google Gen AI Python SDK (신규 통합 SDK) ─────────────
# Gemini 3.5 Flash: google-genai >= 1.55.0 필요
# langchain-google-genai 가 내부적으로 사용
google-genai>=1.55.0
# 구 SDK (호환성 유지 — langchain-google-genai 일부 버전이 참조)
google-generativeai>=0.8.0

# ── 데이터 검증 ───────────────────────────────────────────
pydantic>=2.13.3

# ── 이미지 처리 ───────────────────────────────────────────
pillow>=11.1.0

# ── RAG 벡터DB + 임베딩 ──────────────────────────────────
chromadb>=1.5.9
sentence-transformers>=5.5.1

# ── 웹 검색 (web_utils — DuckDuckGo fallback) ─────────────
duckduckgo-search>=8.1.1
requests>=2.33.1
aiohttp>=3.13.5
httpx>=0.28.1
beautifulsoup4>=4.14.3
